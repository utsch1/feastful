import Head from 'next/head';

export default function Logout() {
  return (
    <div>
      <Head>
        <title>Logout</title>
        <meta name="description" content="Logout" />
      </Head>

      <h1>Logout</h1>
    </div>
  );
}
